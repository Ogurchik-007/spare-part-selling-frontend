import React from 'react';

import classes from './Main.module.scss';

const Main: React.FC = () => {
	return (   
		<main className={classes.main}>
				Slider
		</main>
	);
}
    
export default Main;
